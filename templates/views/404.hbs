<!DOCTYPE html>
<html>
    <head>
        <link rel="icon" src="/img/weather.png">
    </head>
    <body>
        <div class="main-content">
            {{>header}}
            <p>{{errorMessage}}</p>
        </div>       
        {{>footer}}
    </body>
</html>